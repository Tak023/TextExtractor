import AppKit

// This file is a placeholder - we use macOS built-in screencapture instead
// Keeping the file to avoid modifying the Xcode project structure

final class CaptureOverlayWindow: NSWindow {
    // Not used - using macOS screencapture command instead
}
